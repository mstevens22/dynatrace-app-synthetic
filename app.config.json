{
  "environmentUrl": "https://sec51707.apps.dynatrace.com/",
  "app": {
    "name": "dynatrace-app-synthetic",
    "version": "0.0.0",
    "description": "Display availability of chanel.com based on Synthetic scenarios",
    "id": "my.dynatrace.app.synthetic",
    "scopes": [
      { "name": "storage:logs:read", "comment": "default template" }, 
      { "name": "storage:buckets:read", "comment": "default template" },
      { "name": "storage:metrics:read", "comment": "Allows to read metrics" },
      { "name": "storage:entities:read", "comment": "Allows to read entities" },
      { "name": "storage:events:read", "comment": "Allows to read events" },
      { "name": "state:user-app-states:read", "comment": "Allows to store local user data" },
      { "name": "state:user-app-states:write", "comment": "Allows to store local user data" },
      { "name": "state:app-states:read", "comment": "Allows to store local user data" },
      { "name": "state:app-states:write", "comment": "Allows to store local user data" },
      { "name": "settings:objects:read", "comment": "Allows to store local user data" }      
    ]
  }
}